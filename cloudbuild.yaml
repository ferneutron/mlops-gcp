steps:

- name: 'gcr.io/cloud-builders/git'
  args: ['clone', 'https://github.com/ferneutron/mlops-gcp.git']
  id: 'Clone Repository'

options:
  automapSubstitutions: true
  logging: CLOUD_LOGGING_ONLY